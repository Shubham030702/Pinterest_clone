{
    "version": 2,
    "builds": [{"src": "./app.js", "use": "@vercel/node"}],
    "routes": [
        { "src": "/api/users/(.*)", "dest": "/routes/usersRouter" },
        { "src": "/api/(.*)", "dest": "/routes/indexRouter" },
        { "src": "/(.*)", "dest": "/app.js" }
    ]
}
